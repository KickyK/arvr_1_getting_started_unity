using ARVR.ScriptableTypes;
using UnityEngine;

public class EnemyController : MonoBehaviour
{
    public RuntimeList<GameObject> playerList;

    private void Start()
    {
    }

    // Update is called once per frame
    private void Update()
    {
    }
}