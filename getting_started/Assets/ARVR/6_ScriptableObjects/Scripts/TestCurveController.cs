using UnityEditor.UIElements;
using UnityEngine;

public class TestCurveController : MonoBehaviour
{
    public int x;

    // Start is called before the first frame update
    private void Start()
    {
    }

    // Update is called once per frame
    private void Update()
    {
    }
}